# Service Environment
ENVIRONMENT=development
LOG_LEVEL=DEBUG

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_dev_password

# PostgreSQL
DATABASE_URL=postgresql+asyncpg://safety:dev_password_unsafe_only@postgres:5432/safety_db

# Qdrant
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_API_KEY=qdrant_dev_key_unsafe_only

# LLM (use local for now, OpenAI later)
LLM_PROVIDER=local
LOCAL_LLM_ENDPOINT=http://ollama:11434
OPENAI_API_KEY=sk-your-key-here

# ML Model
SCREENING_MODEL=tinybert
SCREENING_THRESHOLD=0.7

# Action Policy
ACTION_POLICY=graduated_escalation

# Observability
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4317

# Auth
JWT_SECRET=super_secret_change_in_production
API_KEYS=test-key,dev-key
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://