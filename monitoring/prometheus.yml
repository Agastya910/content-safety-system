global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'safety-system'
    static_configs:
      - targets: 
        - 'event-ingestion:8000'
        - 'risk-screening:8000'
        - 'embedding-context:8000'
        - 'reasoning:8000'
        - 'action-executor:8000'
        - 'session-manager:8000'
        - 'reporting:8000'
